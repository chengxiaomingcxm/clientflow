Read and follow the ClientFlow V1.0 Master Development Instructions.

We are starting Phase 0 only.

Do not implement Phase 1 or any business functionality.

Your task is to establish the architectural foundation and development environment for ClientFlow.

Phase 0 requirements:

1. Initialize and configure the Next.js + TypeScript application.

2. Configure Tailwind CSS and shadcn/ui.

3. Establish the agreed project directory structure.

4. Configure Supabase client/server integration, but do not implement authentication flows yet.

5. Create the initial PostgreSQL schema and migration strategy for profiles, clients, projects, and tasks.

6. Add appropriate foreign keys, constraints, indexes, timestamps, and ownership fields.

7. Prepare the foundation for Row Level Security, but do not implement Phase 1 authentication behavior.

8. Configure Vitest.

9. Configure Playwright.

10. Configure ESLint and formatting.

11. Add .env.example without real credentials.

12. Add appropriate .gitignore rules.

13. Add basic GitHub Actions CI for lint, type checking, tests, and build where practical.

14. Add a minimal README describing local development setup.

Before modifying files, inspect the repository and produce a Phase 0 implementation plan.

The plan must include:

- proposed architecture

- proposed directory structure

- database schema

- files to create

- files to modify

- dependencies to install

- testing strategy

- security considerations

- risks or assumptions

STOP after producing the plan.

Do not modify any file until I explicitly approve the Phase 0 plan.
